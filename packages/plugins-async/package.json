{
  "name": "@formwright/plugins-async",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "echo \"TODO: lint @formwright/plugins-async\"",
    "test": "echo \"TODO: test @formwright/plugins-async\"",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist build coverage"
  },
  "dependencies": {
    "@formwright/contract": "workspace:*",
    "@formwright/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
